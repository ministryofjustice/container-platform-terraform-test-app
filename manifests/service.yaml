apiVersion: v1
kind: Service
metadata:
  name: hello-world-service
  namespace: starter-pack
spec:
  type: ClusterIP
  selector:
    app: hello-world
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
